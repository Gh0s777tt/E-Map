# Dependencies
node_modules/
.pnpm-store/

# Build / output
dist/
build/
.next/
out/
.turbo/
.expo/
next-env.d.ts
*.tsbuildinfo

# Env & secrets
.env
.env.*
!.env.example
*.pem
*.key

# Vercel (link + lokalne env z pull)
.vercel/

# Supabase
supabase/.branches/
supabase/.temp/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
# Sidecary AppleDouble (`._<nazwa>`): macOS zrzuca do nich rozszerzone atrybuty pliku,
# gdy repo leży na wolumenie bez natywnego wsparcia (exFAT/NTFS/sieciowy) — powstają
# SAME z siebie przy każdej edycji, więc bez tego wzorca wpadają do `git add -A` jako
# binarne śmieci obok realnych plików. Biome (`vcs.useIgnoreFile`) też je wtedy czyta
# i zgłasza „stream did not contain valid UTF-8" jako błąd wewnętrzny.
._*
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code (lokalne: launch.json preview auto-generowany, sesje, ustawienia lokalne)
.claude/

# Testy / coverage
coverage/

# Dokumentacja (MkDocs Material) — build lokalny + venv (docs/api generowane TypeDoc-iem jest commitowane)
site/
.cache/
/.venv/

# Mobile
*.keystore
ios/Pods/
android/.gradle/

# Playwright
test-results/
playwright-report/

# Artefakty buildów mobile
*.aab
*.apk

# Lokalne credentials iOS (NIE commitować)
apps/mobile/credentials/
apps/mobile/credentials.json
*.p12
*.mobileprovision
*.p8
